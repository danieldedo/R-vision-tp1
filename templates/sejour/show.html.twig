{% extends 'base.html.twig' %}

{% block title %}Sejour{% endblock %}

{% block body %}
    <h1>Sejour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sejour.id }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ sejour.debut ? sejour.debut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ sejour.fin ? sejour.fin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sejour_index') }}">back to list</a>

    <a href="{{ path('app_sejour_edit', {'id': sejour.id}) }}">edit</a>

    {{ include('sejour/_delete_form.html.twig') }}
{% endblock %}
